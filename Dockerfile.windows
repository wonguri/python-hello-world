# Wine을 사용한 Windows용 Python 실행 파일 빌드
FROM tobix/pywine:3.11

WORKDIR /wine/drive_c/app

# 소스 코드 복사
COPY test.py .

# PyInstaller 설치 및 빌드
RUN wine pip install pyinstaller && \
    wine pyinstaller --onefile test.py

# 결과물은 dist/ 폴더에 생성됨
CMD ["/bin/bash"]
